<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="mx.x10.filipebezerra.horariosrmtcgoiania.ui.activity.HomeActivity">

    <LinearLayout
        android:id="@+id/home_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar -->
        <include layout="@layout/toolbar" />

        <!-- SuperCardToasts -->
        <LinearLayout
            android:id="@+id/card_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:orientation="vertical" />

        <!-- Fragments Placeholder -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>

    <!-- Left menu drawer -->
    <fragment
        android:name="mx.x10.filipebezerra.horariosrmtcgoiania.ui.navdrawer.LeftDrawerFragment"
        android:id="@+id/left_drawer"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        tools:layout="@layout/fragment_left_drawer"/>

    <!-- Right menu drawer -->
    <fragment
        android:name="mx.x10.filipebezerra.horariosrmtcgoiania.ui.navdrawer.RightDrawerFragment"
        android:id="@+id/right_drawer"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        tools:layout="@layout/fragment_right_drawer" />

</android.support.v4.widget.DrawerLayout>