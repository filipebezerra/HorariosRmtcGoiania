<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="mx.x10.filipebezerra.horariosrmtcgoiania.ui.activity.HomeActivity">

    <LinearLayout
        android:id="@+id/home_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar -->
        <include layout="@layout/toolbar" />

        <!-- SuperCardToasts -->
        <LinearLayout
            android:id="@+id/card_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:orientation="vertical" />

        <!-- Fragments Placeholder -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>

    <!-- Right menu drawer -->
    <ListView
        android:id="@+id/left_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@color/navdrawer_list_divider"
        android:dividerHeight="0.1dp"
        android:listSelector="@drawable/navdrawer_list_selector"
        android:background="@color/navdrawer_list_background" />

    <!-- Left menu drawer -->
    <ListView
        android:id="@+id/right_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:choiceMode="singleChoice"
        android:divider="@color/navdrawer_list_divider"
        android:dividerHeight="0.1dp"
        android:listSelector="@drawable/navdrawer_list_selector"
        android:background="@color/navdrawer_list_background" />

</android.support.v4.widget.DrawerLayout>